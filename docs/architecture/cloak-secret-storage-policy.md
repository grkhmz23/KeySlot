# Cloak Secret Storage Policy

This policy defines the allowed storage and bridge boundaries for GORKH Wallet -> Private.

## Scope

Phase 2.6 supports the Cloak SOL private payment MVP plus read-only private history scan. Reviewed mainnet-only deposits and full-withdraw/private pay remain the only transaction execution paths. Private transfers, partial withdrawals, swaps, standalone compliance export, and batch workflows remain locked.

## Secret Classes

Normal wallet secrets:

- BIP39 mnemonic
- Solana signing seed
- imported private key material
- wallet JSON / keypair arrays

Cloak-specific secrets:

- UTXO private keys
- full notes or full UTXO objects
- viewing keys / `nk`
- nullifier secrets
- proof inputs
- raw scan cache
- decrypted compliance scan contents

## Storage Rules

UserDefaults may store only public metadata and local security settings. It must never store normal wallet secrets or Cloak-specific secrets.

macOS Keychain is the only approved local storage class for wallet signing material. Future Cloak secret storage must use a Keychain-backed private vault or a vault encrypted by a Keychain-protected key.

Current Phase 2.5 private vault behavior:

- serialized spend state is stored only in Keychain
- scan credential material, if generated by the SDK, is stored only in Keychain
- safe metadata may be stored outside Keychain
- no nullifiers are stored outside Keychain
- no proof inputs are stored
- no raw scan cache is stored

## UI Rules

The UI may show:

- public wallet address
- network
- mint address
- amount lamports
- fee quote
- Cloak program id
- request id
- future transaction signature
- redacted commitment prefix
- leaf index
- bridge status

The UI must not show normal wallet secrets or Cloak-specific secrets outside a separately designed authenticated export or recovery flow.

## Logging and Audit Rules

Audit logs may include:

- action kind
- network
- public wallet address
- amount lamports
- mint address
- fee quote
- request id
- status
- future transaction signature
- redacted commitment prefix
- leaf index

Audit logs must not include:

- private keys
- seed phrases
- mnemonics
- wallet JSON
- UTXO private keys
- full UTXOs
- notes
- viewing keys
- nullifier secrets
- proof inputs
- serialized transactions
- transaction bytes
- message bytes
- raw signer bytes
- raw scan cache

## Bridge Rules

Swift may send only safe contract fields to the local helper:

- request id
- command
- action kind
- network
- public wallet address
- amount lamports
- mint address
- program id
- fee quote
- timestamp

Swift must not send:

- private key
- seed phrase
- mnemonic
- wallet JSON
- UTXO private key
- full UTXO object
- note secret
- viewing key
- nullifier secret
- proof input
- serialized transaction payload
- raw signer bytes

Exception: for an already approved full-withdraw/private pay request, Swift may pass the stored Cloak spend state to the fixed local helper transiently if the SDK requires reconstruction of the input UTXO in TypeScript. This value must stay in memory, must not be logged, must not be persisted by the helper, and must not be exposed to Agent/Assistant context.

Exception: for a user-requested read-only history scan, Swift may pass the stored scan credential to the fixed local helper transiently as `scanStateBase64` after wallet unlock and LocalAuthentication. The helper may use it only for `scanTransactions`, must wipe local references where practical, must not log or persist it, and must return only safe summaries.

Phase 2.4 helper invocation is disabled by default and limited to dry-run `health`, `env-check`, and `deposit-plan` when explicitly enabled by internal development policy. Transaction execution commands are locked.
Phase 2.5 enables `execute-deposit` and `full-withdraw` only through the interactive native signer bridge. Phase 2.6 enables `scan` as a read-only command. `partial-withdraw`, `private-transfer`, `swap`, and `compliance-export` remain locked.

The native adapter may invoke only the fixed local helper path and fixed allowlisted Node executable paths. It must not use shell command strings, user-provided executable paths, arbitrary arguments, secret environment variables, or wallet/Cloak secret stdin payloads.

Phase 2.5 permits the helper to call the SDK UTXO deposit/full-withdraw APIs only after Swift approval and only through wallet-adapter callbacks that bounce signing back to Swift. Phase 2.6 permits `scanTransactions` and aggregate `toComplianceReport` summary generation. It must not call private transfer, swap, relay submit, partial withdraw, standalone compliance export, or direct signer APIs.

The helper may return a locked signer request summary for review. This summary may include public wallet address, network, amount, mint, program id, fee quote, purpose text, and draft fingerprint. It must not include transaction bytes, message bytes, serialized transactions, or any payload that can be signed directly.

Swift native wallet code remains the only signing authority. Future `signTransaction` or `signMessage` support must require wallet unlock, LocalAuthentication, signer public key match, network match, action and amount match, Cloak program id match, fee acknowledgement, Shield review, explicit approval, mainnet confirmation phrase, draft fingerprint match, and audit before and after signing.
Phase 2.5 `signTransaction` and `signMessage` requests already require wallet unlock, LocalAuthentication, signer public key match, network match, action and amount match, Cloak program id match, fee acknowledgement, Shield review, explicit approval, exact mainnet confirmation phrase, draft fingerprint match, and audit before and after signing.

The helper must reject or warn on wallet secret-like environment variable names including `PRIVATE_KEY`, `SECRET_KEY`, `SEED_PHRASE`, `MNEMONIC`, `WALLET_JSON`, `CLOAK_VIEWING_KEY`, and `ZERION_TOKEN`. Environment values must not be printed. `SOLANA_RPC_URL` may be checked only as present/missing with a redacted status.

## Scan Cache Strategy

Phase 2.6 stores only safe scan summaries in local cache:

- transaction signature
- timestamp or slot if available
- gross, fee, net, and running balance amounts
- mint and symbol
- redacted commitment prefix
- status
- aggregate totals

Raw decrypted scan material, full UTXOs, notes, nullifiers, proof inputs, and scan credential material must not be written to UserDefaults or audit logs.

The Private UI can clear the scan cache. Cache clear does not delete local spend state, scan credential material, or wallet state. Full vault deletion remains a separate destructive action.

## Agent and Assistant Restrictions

Agent, Assistant, Context, and LLM layers must never receive wallet signing material or Cloak-specific secrets. They may receive only safe summaries such as public address, network, amount, fee quote, request id, status, and transaction signature.

Agent-controlled private wallet execution is not implemented. Future execution must require wallet unlock, LocalAuthentication, Shield review, explicit approval, local signing, send, confirmation, and audit.
